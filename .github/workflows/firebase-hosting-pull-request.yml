steps:
      - uses: actions/checkout@v4
      
      # PASO NUEVO: Instala las dependencias
      - name: Install dependencies
        run: npm ci
        
      - name: Build project
        run: npm run build
        
      - uses: FirebaseExtended/action-hosting-deploy@v0
        # ... resto del archivo igual